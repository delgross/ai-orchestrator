name: memory_backup
type: agent
enabled: false
schedule: "*/1 hours" # Every 1 hour
priority: low
idle_only: true
prompt: "Trigger a full database backup of the project memory."
tools:
  - mcp__project_memory__trigger_backup
description: "Periodic backup of the surrealdb memory store."
