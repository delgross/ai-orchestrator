name: night_shift_refactor
type: agent
enabled: false
prompt: "Execute the following command to force-run the Night Shift PDF ingestion queue: python3 trigger_night_shift.py"
tools:
  - run_command
schedule: "03:00"
priority: low
idle_only: true
description: "Force-run the Night Shift heavy PDF ingestion queue (Modal H100)"
